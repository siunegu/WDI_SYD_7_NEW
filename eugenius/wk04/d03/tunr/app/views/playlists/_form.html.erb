<%= form_for @playlist do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	
  <%= f.label :song_ids, 'Songs' %>
	<%= f.collection_check_boxes :song_ids, Song.all, :id, :name %>

	<%= f.submit %>
<% end %>

